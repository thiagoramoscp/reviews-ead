<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('head'); %>
</head>

<body>
    <%- include('header'); %>
        <main class="main">
            <section class="login-section">
                <div class="login-box">
                    <h1>Login</h1>
                    <% if(messages.error) { %>
                        <span><%= messages.error %></span>
                        <% } else if (message) {%>
                            <span><%= message %></span>
                            <% } %>
                                <form action="/auth/login" method="post">
                                    <div>
                                        <label class="auth-labels" for="email">E-mail:</label>
                                        <input type="email" name="email" id="email" required>
                                    </div>
                                    <div>
                                        <label class="auth-labels" for="password">Senha:</label>
                                        <input type="password" name="password" id="password" required>
                                    </div>
                                    <button type="submit">Entrar</button>
                                </form>

                </div>
                <p>Novo na Central dos Cursos? <a href="/auth/register">Crie a sua conta!</a></p>
            </section>
        </main>

        <%- include('footer'); %>
</body>

</html>